pub mod generators;
pub mod loader;


/*
fn main() {
    let weapon_loader = loader::Weapon::from_file("path_to_file");
    // let weapon_loader = loader::Weapon::from_files(vec!["p1", "p2"]);
    let weapon = WeaponGenerator::load(weapon_loader.content());

    let w1: GeneratedWeapon = weapon.generate();
}

*/